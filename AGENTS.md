# AGENTS.md

## 产品定位

- 本目录是 Pixel Local 的正式开发目录；目录名虽然是“营销图片编辑器”，产品名统一使用 Pixel Local。
- 本产品归属于父项目 `Project_07_Pixel创意工具`。
- Pixel Local 是本地优先的营销图片编辑器，也提供 Codex Skill、MCP Server 和 Bridge 来操作真实画布。
- 先遵守项目根目录 `AGENTS.md`，再遵守本文件。

## 系统边界

- 编辑器页面、Bridge、MCP Server、Codex Skill 是四个可独立失败的层，不用其中一层的成功替代其他层验证。
- 编辑器默认运行于 `http://localhost:3000`；Bridge 默认监听 `127.0.0.1:43127`，不得暴露到公网。
- `editor_status.ready=true` 只能证明 Bridge 存在已连接客户端，不能证明页面服务可访问或用户可见页面已经打开。
- 项目和模板默认保存在浏览器 IndexedDB；真实画布、`.pixel.json`、运行日志、QA 截图和受控素材不属于公开源码。

## 默认开发与验证

1. 先判断修改属于编辑器 UI、画布模型、命令协议、Bridge、MCP、Skill、安装器还是运维脚本。
2. UI 和画布功能修改正式源码；不要把构建目录或运行缓存当作开发入口。
3. 运行直接相关的测试，再按范围运行 `npm run build`、`npm test` 或专项 Bridge / Sidebar 测试。
4. 页面功能必须验证 `http://localhost:3000` 可访问并检查真实交互。
5. 自动化能力必须通过 MCP 读取真实画布；发生写入时先读取 revision，写入后再次回读状态。

## Bridge 与画布写入

- 直接写入使用唯一 `requestId` 和最新 `expectedRevision`，避免重复执行和覆盖并发修改。
- 请求超时后先使用任务查询能力确认原操作状态，不生成新的写入请求盲目重试。
- 判断服务可用时分别检查页面 HTTP、Bridge `/health`、端口监听和主画布连接状态。
- “打开编辑器”只有在页面真实可访问、用户可见且画布重新连接后才算完成。

## 设计与导出验收

- 设计结果必须存在于真实可编辑画布中，不以代码、命令返回或预览截图替代。
- 检查页面、Frame、图层、文字、图片、尺寸和层级；关键写入做结构化回读。
- 交付图片时验证 PNG / JPG 文件真实存在、尺寸正确、可打开，且来源 Frame 与用户目标一致。
- QA 使用临时页面、Frame 或项目；完成后按任务约定清理，不污染用户主画布。

## 安装、系统与发布边界

- 修改 LaunchAgent、Codex 配置、已安装 Skill 或其他系统级状态前必须获得明确授权。
- 安装完成不等于运行可用；继续验证进程、端口、页面、MCP 工具和真实画布回读。
- 公开发布前排除本地画布、素材、运行日志、备份、环境变量、密钥和测试产物。
