# 中性演示流程

这个练习不依赖任何品牌、客户或生成图片。

1. 启动 Pixel Local，新建页面“演示”。
2. 新建一个 800×800 Frame，背景设为浅灰色。
3. 添加一个深蓝色圆角矩形，尺寸约 640×180，放在 Frame 中部。
4. 添加标题“LOCAL DESIGN”，使用白色粗体并居中。
5. 再添加一行较小文字“Edit locally. Export freely.”。
6. 在 Layers 中折叠和展开 Frame 内容，确认画布不受影响。
7. 导出 Frame 为 PNG。

完成后即可继续尝试多页面、图片导入、编组、对齐和项目文件保存。
